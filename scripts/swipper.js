const swiper = new Swiper('.swiper', {
  // Optional parameters
  loop: true,
  autoheight: true,
  slidesPerView: 0.8,

  // If we need pagination
  pagination: {
    el: '.swiper-pagination',
    clickable: true,
  },



});
